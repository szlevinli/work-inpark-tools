---
description: 
globs: 
alwaysApply: true
---
# 开发指南

## 环境设置
- 使用 Python 3.12
- 使用 `uv` 作为包管理工具
- 使用 `.venv` 作为虚拟环境目录

## 代码风格
- 使用类型注解
- 使用 VS Code Ruff 扩展进行代码格式化和质量检查
- 遵循 PEP 8 编码规范
- 行长度限制为 80 字符

## 项目结构规范
- 源代码放在 `src/work_inpark_tools` 目录下
- CLI 工具实现放在 `cli` 子目录中
- 输出文件统一放在 `output` 目录下

## 文档规范
- 所有函数都应该有文档字符串
- 复杂的逻辑需要添加详细的注释
- README 需要包含安装和使用说明

## 测试规范
- 新功能需要编写对应的测试用例
- 测试文件应该放在相应模块的 `tests` 目录下
- 运行测试前确保虚拟环境已激活

## IDE 配置
- 安装 VS Code Ruff 扩展（charliermarsh.ruff）
- 启用保存时自动格式化
- Ruff 配置已在 `pyproject.toml` 中定义：
  - 代码风格检查规则
  - 导入排序
  - 类型注解检查
  - 代码质量规则

## 代码质量检查
- 使用 `ruff` 进行代码质量检查
- 提交代码前运行 `ruff check` 确保代码符合规范
- 建议在编辑器中配置保存时自动运行 `ruff format`

## 新增功能开发原则
- 新增功能模块时，需尽可能满足软件工程的通用性、可扩展性、可维护性等原则，优先泛化设计
- 除非有明确要求，不要修改现有项目中的任何文件、类、方法或函数，尽量避免对现有代码的变更
- 所有新功能应以最小侵入、最大复用为目标，保持项目结构清晰和可维护性
